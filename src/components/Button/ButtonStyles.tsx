import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  button: {
    backgroundColor: '#111',
    padding:10,
    borderRadius: 5
  },
  text: {
    color: '#fff',
    fontWeight: 'bold'
  }
});